node_modules
dist
.next
.turbo
coverage
pnpm-lock.yaml

# Canon docs + living demo references — never auto-reformat (protects the two
# demo HTMLs and the numbered spec pack from prettier drift).
docs/

# Vendored upstream skill — same argument as docs/, one step stronger. It ships
# its own LICENSE, its own .github/workflows/validate.yml and its own
# scripts/validate-package.py, so reformatting our copy diverges it from
# upstream and puts it under a validator we do not run. CLAUDE.md's Copy style
# section cites it as an authority; an authority we have quietly restyled is a
# worse authority. Added 2026-08-24, when `pnpm format:check` was found red on
# an untouched tree for README.md and SKILL.md.
.agents/skills/humanizer/

# Binary assets
public/
SAHODA-ASSETS/
brand/

# Local visual-verification scratch (see .gitignore).
.ui-port-shots/

# Playwright run artifacts. Already gitignored, but prettier reads only this
# file, so without these two lines a smoke run leaves the tree in a state where
# the documented format gate fails on generated JSON it does not own.
apps/web/test-results/
apps/web/playwright-report/

# The gate runner writes .gate/verdict.json and one log per stage. Prettier does
# not read .gitignore, so without this line stage 4 lints the artifact stage 1
# wrote — the gate failing on its own output.
.gate/
