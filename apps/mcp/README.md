# apps/mcp — BACKLOG #18 (placeholder)

The MCP server (OS exposed as agent tools) is post-Alpha backlog item #18. This directory is a
placeholder so the tree matches Build Companion §8; it has **no `package.json`**, so pnpm does
not treat it as a workspace and it adds zero install/build overhead.

Do not build here during the Alpha sprint.
