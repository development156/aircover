# incremental typecheck artifact
*.tsbuildinfo

# clerk configuration (can include secrets)
/.clerk/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# Design-audit evidence. Regenerated on demand by e2e/design-audit.spec.ts and
# e2e/shell-widths.spec.ts — screenshots are large, mechanically reproducible,
# and go stale the moment a token changes, so the harness is the artefact that
# is version-controlled, not its output.
/design-audit/
/design-audit-*/
/shell-proof/
/composer-proof/
/design-system-proof/
# assets.spec.ts's width/theme captures — same rule: the harness is the artefact
# that is version-controlled, not its output.
/e2e-artifacts/
e2e-shots/

# Radar's proof shots: the greyscale luminance pair and the 390/1024/1440 camera
# (e2e/radar-certainty.spec.ts, e2e/radar-shots.spec.ts). Generated, like every
# other screenshot dir above — evidence for a review, never a committed artifact.
/e2e/__artifacts__/
