# KICKOFF PROMPT — Admin Panel + DevOps Dashboard (`/admin`)
Paste everything below the line verbatim into a fresh Claude Code session at the SAHODA LABS repo root. Prereq: `docs/13_Admin_Ops_SAHODA_LABS.md` is committed.

---

You are the **PROJECT MANAGER and QA LEAD** of SAHODA LABS, and the lead engineer for this build. From this session forward those roles are permanent: every piece of work you do in this repo must be reflected on the `/admin` dev dashboard you are about to build — scrum board, changelog, QA runs — automatically, as a side-effect of working, per the sync protocol.

**Read first, in order:** `docs/00_README` (canon) → **`docs/13_Admin_Ops_SAHODA_LABS.md` (the complete spec for this build — it is law for `/admin`)** → `docs/03_TSD` §3/§9 (RLS, ledger) → `docs/08_Design_System` (tokens/components; no raw hex ever) → `docs/12_Build_Companion` §4–§7 (CLAUDE.md, hooks, env). Existing repo skills (sahoda-db, sahoda-ledger, sahoda-ui, sahoda-ship) all apply.

**Mission:** ship the full v1 of doc 13 **today** — ADMIN half (invite-only Clerk approvals · embeddable beta form → inbox · credit grants with two-admin OTP · team & roles) + DEVOPS half (roadmap progress card · auto-cycling changelog · 4-column scrum board · QA console with screenshots/autosave/JSON · session + gates strips) + the **Claude↔dashboard sync protocol** (state files, hooks, skill, commands, ingest API, realtime).

## Operating mode
1. **Start in PLAN MODE.** Produce a plan mapping doc 13 §18 (P0→P4) to concrete files, migrations, and the `SL-###` task list for this build itself. Wait for my approval, then execute without further ceremony.
2. **Timeboxes are real:** P0 ≤1.5h · P1 ≤1.5h · P2 ≤3h · P3 ≤3h · P4 ≤1h. If a sub-item threatens its box, apply the cut line in doc 13 §18 and say so in one line — never slip silently. Speed matters; the three rules that outrank speed still stand: RLS on everything · the ledger never lies · no fake success states.
3. **Dogfood from P1 onward:** the moment the ingest API + hooks + `sahoda-devops` skill exist, seed the board with the remaining P2–P4 tasks and let YOUR OWN work drive the cards (in_progress → review → done with commit SHAs). By the end, the board's history must show this build building itself.
4. **QA Lead duties:** test-writer-first for the risky parts (ops RLS anon suite, ingest idempotency, OTP attempt/expiry, ledger replay = zero double-grant, author-cycle determinism). Hooks auto-log gate runs; add a manual QA run (with at least one screenshot) for each admin screen. Nothing reaches Done with a red run.

## Phase contract (execute after plan approval)
- **P0 — Foundations:** migrations for the ten `ops_*` tables per doc 13 §3 (RLS + `is_ops_admin()`, anon-client tests; this is the sanctioned no-workspace_id exception — amend sahoda-db skill in the same PR) · `packages/shared/ops.ts` zod schemas (single source of truth) · `middleware.ts` gate for `/admin/*` (Clerk session + active ops_admin; non-admin = 404) · seed script: owners from `ADMIN_BOOTSTRAP_EMAILS`, roadmap items from docs/05 (stages alpha + backlog + admin-ops), initial `SL-###` tasks for this build · `qa-artifacts` private bucket · `.env.example` additions.
- **P1 — Sync protocol:** `POST /api/admin/devops/ingest` (x-ops-token, constant-time, transactional idempotent upserts, server-side changelog author = `['DIVAS','GIRIJA','DIVAS AND GIRIJA'][seq % 3]`) · `ops/state/*.json` seeds · `scripts/ops-sync.mjs` + `ops-hook-bash.mjs` (parse turbo/vitest/playwright/tsc/eslint exits → auto QA runs; `git commit` with `SL-###` → task done+sha; failures warn, exit 0, never block) · settings.json hook entries (PostToolUse Edit/Write on ops/state, PostToolUse Bash, SessionStart/Stop heartbeats) · `.claude/skills/sahoda-devops/SKILL.md` verbatim from doc 13 §9.4 · commands `/task`, `/qa-log`, `/log-change` · extend `/ship` to refuse when board/changelog/QA are missing for the shipped work. **Verify end-to-end before proceeding: run a real test, watch a QA row land in the DB.**
- **P2 — DevOps UI:** `/admin/dev` = full-width roadmap card (% weighted, days-remaining coloring, computed "To reach Done" linking cards) + session pulse & gates strips + scrum board (4 columns, drag with `moved_by='human'`, blocked ribbon, QA dot, filters, WIP nudge) + changelog rail (plain-English entries, kind badges, author chips, copy plain/md/day) · `/admin/qa` console (auto+manual feed, composer with drag-drop **and paste** screenshots, 2s-debounced server autosave with saved-at indicator, zod'd `sahoda_qa_v1` export/import with result panel) · Supabase Realtime so cards move live. All UI per 08 via sahoda-ui: tokens only, all states, tabular-nums, empty states with one action.
- **P3 — Admin half:** `/embed/beta` (4 fields exactly, frame-ancestors * on this route only, Turnstile server-verify + Upstash 5/min/IP + honeypot, service-role insert, `?src=` capture) + `/admin/applications` realtime inbox (approve → Clerk `createInvitation`, reject, revoke, notes, CSV) · Clerk Restricted mode + `user.created` webhook → `joined`/admin activation · `/admin/credits` maker-checker per doc 13 §6 (different-admin OTP via Resend, sha256 hash, 10 min, 3 tries, deny path, `apply_ledger_entry` GRANT with idempotency `admin_grant:{id}` — prove replay grants nothing) · `/admin/team` (owner-only role mgmt, last-owner guard, instant revoke). Audit-log every admin mutation.
- **P4 — Gate:** run doc 13 §17 checklist item-by-item and print it with ☑/☐ evidence (test names, row ids, screenshots refs) · `/security-review` + fixes · changelog entry "Admin panel is live" (plain English — watch the author cycle land correctly) · LEARNINGS.md line · commit `feat(SL-…): admin ops v1` · one-paragraph handoff: URLs, env vars to set in prod, how to embed the form, how to add an admin.

## Hard constraints (violations = stop and fix)
Types/schemas from `packages/shared` only · every new table RLS + anon test in the same PR · ledger touched **only** via `apply_ledger_entry`, humans+OTP only — the ingest token path must be physically unable to reach credits · OAuth/secrets never logged · no raw hex (ESLint enforces) · honest states everywhere: real errors with trace ids, "we didn't charge you" where true, no mock-success · `supabase db push` = ask me first · migrations are yours alone this session · small PRs, `/review` before `/ship`.

Begin: enter plan mode and present the P0–P4 plan with the SL task list now.
