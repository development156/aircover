# packages/render — BACKLOG #7 (placeholder)

The Studio renderer (Satori/ResVG + node-canvas, zero-COGS exports) is post-Alpha backlog item
#7. This directory is a placeholder so the tree matches Build Companion §8; it has **no
`package.json`**, so pnpm does not treat it as a workspace.

Do not build here during the Alpha sprint.
